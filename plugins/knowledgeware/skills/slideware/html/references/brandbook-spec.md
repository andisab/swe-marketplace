# Brandbook spec — moved

The canonical brandbook spec (prose layer + YAML frontmatter schema + per-format appendices) lives in the brandware skill: **`skills/brandware/references/brandbook-spec.md`**. The html-specific material from this file is preserved there as **Appendix B** (token → CSS variable mapping, Google Fonts URL generation).
